<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <title>NIS Restaurant - הזמנת שולחן</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    
    <link href="/css/global.css" rel="stylesheet" type="text/css">
    <link href="/css/reservation.css" rel="stylesheet" type="text/css">
</head>

<body>

    <%- include('includes/header') %>
    <%- include('includes/navbar') %>

    <div class="content-wrapper">
        <main class="main d-flex justify-content-center align-items-center">
            <div class="reservation-card">
                <h2 class="res-title text-center">הזמנת שולחן ב-NIS</h2>
                <p class="res-subtitle text-center">נשמח לראות אתכם! מלאו את הפרטים ונשמור לכם מקום.</p>

                <form action="/save_res" method="post" id="resForm" novalidate>
                    
                    <input type="hidden" id="selectedSeat" name="seat" value="">
    
                    <input type="hidden" name="email" value="<%= (typeof user !== 'undefined' && user) ? user.email : '' %>">

                    <div class="selection-row">
                        <div class="select-group">
                            <label for="num_of_people"><i class="fas fa-users"></i> כמה תהיו?</label>
                            <input type="number" class="res-input" id="num_of_people" name="num_of_people" min="1" max="20" title="נא להזין מספר בין 1 ל-20" placeholder="כמות" required>
                        </div>
                        <div class="select-group">
                            <label for="date"><i class="fas fa-calendar-alt"></i> באיזה יום?</label>
                            <input type="date" class="res-input" id="date" name="date" required onkeydown="return false">
                        </div>
                        <div class="select-group">
                        <label for="time"><i class="fas fa-clock"></i> באיזו שעה?</label>
                        <select class="res-input" id="time" name="time" required>
                        <option value="" disabled selected>בחרו שעה</option>
                        </select>
                        </div>
                    </div>

                    <div class="selection-row mt-4">
                        <div class="select-group">
                            <label for="celebration"><i class="fas fa-gift"></i> מה חוגגים?</label>
                            <select id="celebration" name="celebration" class="res-input" required>
                                <option value="" disabled selected>בחרו סוג אירוע</option>
                                <option value="יום הולדת">יום הולדת 🎂</option>
                                <option value="יום נישואין">יום נישואין ❤️</option>
                                <option value="קידום בעבודה">קידום בעבודה 💼</option>
                                <option value="ערב חברים">סתם יוצאים לכיף 🍻</option>
                                <option value="דייט">דייט רומנטי ✨</option>
                            </select>
                        </div>
                        <div class="select-group flex-grow-1">
                            <label for="phone" id="phoneLabel"><i class="fas fa-phone"></i> טלפון לחזרה</label>
                            <input type="tel" class="res-input" id="phone" name="phone" placeholder="05X-XXXXXXX" pattern="[0-9]{10}" title="נא להזין מספר טלפון בעל 10 ספרות, ללא מקפים" required maxlength="10">
                        </div>
                    </div>

                    <div class="extra-options text-center mt-4">
                        <label class="kids-checkbox">
                            <input type="checkbox" name="kids" value="1">
                            מגיעים עם ילדים? 👶
                        </label>
                    </div>

                    <div class="text-center mt-5">
                        <button type="submit" class="find-table-btn">מצאו לי שולחן</button>
                    </div>

                    <div id="tableSuggestions" class="suggestions-hidden">
                        <hr class="suggestion-divider">
                        <h4 class="text-center mb-4">איפה תרצו לשבת?</h4>
                        <div class="slots-container">
                            <div class="time-slot">
                                <span class="slot-time">בפנים</span>
                                <span class="slot-desc">אווירה אינטימית</span>
                                <button type="button" class="btn-select-slot" onclick="submitFinalForm('בפנים')">בחירה</button>
                            </div>
                            <div class="time-slot recommended">
                                <div class="ribbon">מומלץ</div>
                                <span class="slot-time">מרפסת</span>
                                <span class="slot-desc">אוויר ונוף</span>
                                <button type="button" class="btn-select-slot" onclick="submitFinalForm('בחוץ')">בחירה</button>
                            </div>
                            <div class="time-slot">
                                <span class="slot-time">על הבר</span>
                                <span class="slot-desc">קרוב לאקשן</span>
                                <button type="button" class="btn-select-slot" onclick="submitFinalForm('בר')">בחירה</button>
                            </div>
                        </div>
                    </div>

                    <div class="text-center mt-4">
                        <a href="/" class="back-home-link">חזרה לדף הבית</a>
                    </div>
                </form>
            </div>
        </main>
    </div>
    <%- include('includes/footer') %>
    <%- include('includes/popup') %>

    <script>
       <span  id="user"> const isUserLoggedIn = <%= (typeof isLoggedIn !== 'undefined' && isLoggedIn) ? 'true' : 'false' %>; </span>
    </script>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/global.js"></script>
    <script src="/js/reservation.js"></script>

</body>
</html>